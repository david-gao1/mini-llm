# changes/（预留）

进行中的变更可在此按 `openspec/changes/<change-id>/` 建目录，典型包含：`proposal.md`（提案）、`design.md`（技术设计）、`tasks.md`（实施检查项）、以及 delta `specs/`（相对主规格的增量说明）。  
暂无活动变更时本目录可仅保留本说明；总述见上级 [`README.md`](../README.md)。
