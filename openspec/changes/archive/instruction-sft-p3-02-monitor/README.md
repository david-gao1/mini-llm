# archive · instruction-sft-p3-02-monitor

**状态**：已合并进现行 [`specs/instruction-sft/spec.md`](../../../specs/instruction-sft/spec.md)（需求 + **技术设计** + **实施任务**）。

本目录仅作 **评审与留痕**：变更动机见 `proposal.md`，实现边界见 `design.md`，检查项见 `tasks.md`。日后若在主线规格上做大改版，可复制此目录为新的 `openspec/changes/<id>/` 模板。
