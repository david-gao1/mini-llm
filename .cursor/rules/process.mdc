---
description: 一人全栈流程规范——产品/开发/测试三角色闭环，AI 根据当前角色调整行为
globs:
alwaysApply: true
---

# 一人全栈协作规则

用户一人扮演产品、开发、测试三个角色。AI 必须识别当前角色并遵循对应流程。
完整流程文档见 `PROCESS.md` 及 `docs/process/` 目录。

## 角色识别

通过用户的语言判断当前角色：

| 信号 | 角色 | AI 模式 |
|------|------|---------|
| "我想做…" "需要一个…" "需求是…" "帮我分析…" | **产品** | 追问、拆解、不写代码 |
| "帮我实现…" "写一个…" "改这个…" 或直接给编码任务 | **开发** | 对照 REQ、写代码+测试、遵守规范 |
| "帮我测试…" "跑一下测试" "这个对不对" | **测试** | 运行测试、找边界、不改实现 |

不确定时，**直接问用户**当前是哪个角色。

## 产品模式规则

1. **追问而非假设**：需求不清时主动问，不自作主张
2. **输出 REQ 条目**：帮用户把想法拆解成 HARNESS.md 格式的需求
3. **不写代码**：产品阶段只输出文档和需求分析
4. **评估可行性**：从技术角度给建议，决策权在用户
5. **对齐现有体系**：新 REQ 必须与 HARNESS.md 中的编号、分层、依赖一致

## 开发模式规则

1. **先确认 REQ**：动手前确认要实现的需求条目和契约
2. **遵守编码规范**（见 `docs/process/development.md`）：
   - 公开函数必须有类型标注和 docstring
   - 变量命名 snake_case，类 PascalCase
   - 不硬编码魔法数字，配置从 config.json 读取
3. **同时写测试**：实现代码时同步编写对应测试
4. **小步交付**：不一口气写 500 行，分模块逐步实现
5. **commit 规范**：`<类型>(<范围>): <简述>`，类型包括 feat/fix/refactor/test/docs/chore
6. **不改需求**：发现需求有问题时提出来，但不自作主张改

## 测试模式规则

1. **对照 HARNESS**：先看 REQ 的通过判据，确认测试目标
2. **补充边界用例**：主动提出可能遗漏的边界情况
3. **运行测试**：帮用户执行 pytest 并分析结果
4. **不改实现**：测试阶段只写/改测试代码，不改实现（除非是明确的 bug fix）
5. **记录问题**：发现的问题记录为 Bug 条目或 TODO

## 通用规则

1. **产出物落地**：讨论结果必须落到文档或代码中，不停留在对话里
2. **对齐 HARNESS**：任何改动都要考虑是否影响 HARNESS.md 中的契约
3. **文档同步**：改了代码就提醒用户更新对应文档
4. **技术债标记**：发现"先凑合"的地方，用 `TODO(<关联REQ>): <描述>` 标记

## 关键文件路径

- 流程总纲：`PROCESS.md`
- 需求与验收：`HARNESS.md`
- 项目配置：`configs/config.json`
- 源码：`src/mini_llm/m01_tokenizer/` ~ `m05_generate/`
- 测试：`tests/`
- 设计文档：`docs/`
- 流程文档：`docs/process/`
